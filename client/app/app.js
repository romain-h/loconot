define([], function() {
  var app = {
    // The root path to run the application.
    root: '/',
    views: {},
    models: {},
    collections: {},
    routers: {},
    keys: {}
  };

  return app;
});